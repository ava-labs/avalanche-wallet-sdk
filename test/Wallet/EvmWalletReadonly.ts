import { EvmWalletReadonly } from '@/Wallet/EVM/EvmWalletReadonly';

describe('EVM Wallet Readonly', () => {
    // let publicKey;
    // let wallet = new EvmWalletReadonly();

    it('can return address', () => {
        expect(true).toBe(true);
    });
});
